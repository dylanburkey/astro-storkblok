---
import { storyblokEditable } from '@storyblok/astro'

const { blok } = Astro.props
---

<div {...storyblokEditable(blok)} class="teaser">
  <h2>{blok.headline}</h2>
  <p>{blok.description}</p>
  {blok.link && <a href={blok.link.url}>{blok.link.text}</a>}
</div> 